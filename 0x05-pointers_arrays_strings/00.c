#include "main.h"
/**
 * main - Solve me
 * 
 * Return: Always 0.
 */

char *_strcpy(char *dest, char *src)
{
int i= 0:
int j;
while (src[i] != '\0')
i++;
for (j=0; j <= i +1; j++)
{
*dest = *dest + src[j];
}
}
